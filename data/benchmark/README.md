# Benchmark Data

This directory contains LabInstrumentVQA task files and local assets.

Current starter files:

- `sample_tasks.jsonl`: two runnable demo tasks.
- `images/`: small SVG demo displays used by the sample tasks.

Task image paths are relative to the JSONL file. Keep new task sets near their image assets so files remain portable.
